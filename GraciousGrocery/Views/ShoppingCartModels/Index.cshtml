@model IEnumerable<GraciousGrocery.Models.ShoppingCartModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
@*
    <ul>
        @foreach (var item in Model)
        {

        }
    </ul>
*@
@Html.ActionLink("Place Order", "Order", new { Model }, htmlAttributes: new { @class = "btn btn-dark" })